
  # Design UI/UX (Community)

  This is a code bundle for Design UI/UX (Community). The original project is available at https://www.figma.com/design/FSPOWShHtVL51S1BVebN6F/Design-UI-UX--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  